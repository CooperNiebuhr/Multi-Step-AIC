% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_branching_by_step.R, R/utils.R
\name{plot_branching_by_step}
\alias{plot_branching_by_step}
\title{Visualize branching by step in a path_forest}
\usage{
plot_branching_by_step(forest)

plot_branching_by_step(forest)
}
\arguments{
\item{forest}{An object of class \code{"path_forest"} returned by \code{build_paths()}.}
}
\value{
Invisibly returns a data frame with columns \code{step} and
\code{n_models}. Called for its side-effect of producing a plot.

Invisibly returns a data frame with columns \code{step} and
\code{n_models}. Called for its side-effect of producing a plot.
}
\description{
This helper summarizes how many distinct models are present
on each frontier (step) of the multi-path forward search.
A larger number of models at a given step corresponds to
more branching at that depth of the search.

This helper summarizes how many distinct models are present
on each frontier (step) of the multi-path forward search.
A larger number of models at a given step corresponds to
more branching at that depth of the search.
}
